documentation:
  stage: postprocess
  image: "juliagpu/julia:v0.7-${CI_IMAGE_TAG}"
  dependencies:
    - test:v0.7
  script:
    - julia --project -e 'using Pkg;
                          Pkg.add("Documenter")'
    - julia --project docs/make.jl
